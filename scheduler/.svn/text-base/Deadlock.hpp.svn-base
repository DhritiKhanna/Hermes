#ifndef __DEDALOCK__
#define __DEADLOCK__

#include "Mo.hpp"
#include "Wu.hpp"
#include "InterleavingTree.hpp"

// struct CBComp{
//   bool operator ()(const CB & fst, const CB &snd) const
//   {
//     if(fst._pid < snd._pid || (fst._pid == snd._pid && fst._index < snd._index))
//       return true;
//     else 
//       return false;
//   }
// };

// typedef std::vector<std::set<CB> > ASL; // ample-set at each r*
// typedef std::multimap<int, ASL> CEM; // co-enabled map
// typedef std::vector<std::set<CB> >::iterator ASLIterator;  
// typedef std::multimap<int, ASL>::iterator CEMIterator;  

class Deadlock{
  
public: 
  Deadlock(){}
  ~Deadlock(){};
  
 
};


#endif
